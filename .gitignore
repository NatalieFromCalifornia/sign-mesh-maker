node_modules/
dist/
*.tsbuildinfo
.env
# Exception: the Firebase web config, which is public by design and must reach
# the Cloudflare build. Narrow on purpose — a stray .env anywhere else stays
# ignored. See the header in that file.
!apps/web/.env
.env.local
.firebase/
firebase-debug.log
*.log
.DS_Store

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Third-party artwork used for manual testing. Kept out of the repo: unclear
# licensing, and the repo is public.
/deadpool_wolverine_poster.png
/woman_in_bathtub.svg
/PinchHazardSign.svg

# Sample export produced for manual slicer testing.
/sample-export.3mf
